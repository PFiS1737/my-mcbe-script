import type { BlockConfig } from "./_types"

export default {
  drops: [
    {
      dig_by: "<default>",

      raw: {
        item_id: "minecraft:crimson_stem",
      },
    },
  ],
} as BlockConfig
