import type { BlockConfig } from "./types"

export default {
  drops: [
    {
      dig_by: "<default>",

      raw: {
        item_id: "minecraft:mangrove_log",
      },
    },
  ],
} as BlockConfig
