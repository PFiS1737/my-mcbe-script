name: Build and Push

on:
  push:
    branches:
      - main

jobs:
  main:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 17
          
      - name: Install dependencies
        run: npm install
        
      - name: Build project
        run: gulp
        
      - name: Push to dist branch
        run: |
          git config --global user.name "PFiS1737"
          git config --global user.email "3165823482@qq.com"
          git checkout -b dist
          git add -f dist/
          git commit -m "Auto-push dist folder"
          git push origin dist